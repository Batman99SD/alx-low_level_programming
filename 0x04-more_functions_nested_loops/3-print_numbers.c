#include "main.n"
/**
 * print_numbers - print_numbers: print number followed by new line.
 *
 * Return: return 0.
 */
void print_numbers(void)
{
	int n = 48;
	for (n = 48; n <= 57; n++)
	{
		_putchar(n);
	}
	_putchar(10);
}
