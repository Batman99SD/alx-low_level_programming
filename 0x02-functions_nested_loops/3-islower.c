#include "main.h"
/**
 * _islower - _islower: print 1 if lowercase.
 *  Return: return 1 if 'c' is lower
 *          otherwise always 0.
 **/
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
return (0);
}
